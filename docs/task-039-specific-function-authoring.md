# Task 039 — explicit specific-function authoring

This task adds an optional explicit structure document, typed specific-function
and specific-exchange destinations, structural scaffold origins, and fail-closed
materialization. Structure order is retained after profile-required functions.
No grouping, IDs, names, direction, mandate, topic, LoM, timing, Capability, or
standard role is inferred. UCI resolution remains a subsequent separate step.
